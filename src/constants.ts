import { loyalists } from './data/loyalists'

export const MIN_GEN = 4
export const MAX_GEN = loyalists.length

// Overscroll moves at this fraction of the normal speed beyond bounds
export const OVERSCROLL_FACTOR = 0.3
